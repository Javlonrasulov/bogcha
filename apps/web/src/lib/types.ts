/**
 * API javob tiplari `@bogcha/shared` paketida saqlanadi — web va mobil
 * ilovalar bitta shartnomadan foydalanadi. Bu fayl mavjud `@/lib/types`
 * importlarini o'zgartirmaslik uchun qayta eksport qiladi.
 */
export type * from '@bogcha/shared';
